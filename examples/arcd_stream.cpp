#include <arcd.h>

namespace
{
	/*
	struct adaptive_model
	{
		unsigned short *freq;
		unsigned sum;
	};

	void adaptive_model_create(adaptive_model *const m, const unsigned size)
	{
		m->freq = (unsigned short *)malloc(sizeof(freq[0]) * size);
		for (unsigned i = 0; size > i; ++i)
		{
			m->freq[i] = 1;
		}
		sum = size;
	}

	void adaptive_model_free(adaptive_model *const m)
	{
		free(m->freq);
	}

	void adaptive_model_getprob(arcd_char_t ch, arcd_prob *prob, void *model)
	{

	}

	void adaptive_model_getch(arcd_range_t v, arcd_range_t range, arcd_prob *prob,
							  void *model)
	{

	}
	*/
}

int main(int argc, char *argv[])
{
	(void)argc; (void)argv;
	return 0;
}
